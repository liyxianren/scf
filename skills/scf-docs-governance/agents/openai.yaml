interface:
  display_name: "SCF Docs Governance"
  short_description: "Route SCF docs updates, version facts, and handoff changes."
  default_prompt: "Use $scf-docs-governance to decide which SCF docs and entry files need updating for this change."

policy:
  allow_implicit_invocation: true
