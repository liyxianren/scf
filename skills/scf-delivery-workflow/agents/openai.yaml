interface:
  display_name: "SCF Delivery Workflow"
  short_description: "Model SCF delivery stages, milestones, templates, and risk boards."
  default_prompt: "Use $scf-delivery-workflow to plan SCF project-delivery models, milestone templates, and risk-board behavior."

policy:
  allow_implicit_invocation: true
